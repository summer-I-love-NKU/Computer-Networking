定时器很简单，自己不需要实现？

header:2位seq 8位二进制数？
expectedseqnum：  接收端的seq用途


GBN：

发送端：

    Send_Buf_Queue：发送缓存队列，发送的都入队列，收到ack的就出队列

    GetACKnum()：获取收到的ACKnum要知道具体值（虽然应该也是按序的会不会有跳跃的有趣情况呢），
    可以使用TCP技术（因为累计确认的保证）->促使窗口滑动

    while循环进入：
        0.（检查序列号不溢出，采用简单倒退方式）
        1.填满窗口，打包、发送（根据不等式p_NextSeqNum<p_Base+WINDOW_WIDTH&&read_flag）
        2.准备接收
            收不到ACK，超时重传
            正确接收ACK，滑动窗口——>只需要base变化即可
    
    
            
接收端：
    CheckSEQ_EXPECTED函数保证收到按序的数据包，相对于3-1进行失序处理（丢弃）即可



超时处理呢？？？
